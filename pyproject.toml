[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "diabetes-rag"
version = "2.0.0"
description = "Diabetes Clinical RAG System"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["backend*"]
